import React from 'react';

// 简体中文：新版更新提示已由全局 NotificationToast 接管，以确保在大卡片模式下一视同仁，同时避免在 InfiniteCanvas 中被遮挡。
const UpdateNotification: React.FC = () => {
    return null;
};

export default UpdateNotification;
